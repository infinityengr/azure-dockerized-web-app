# Project Screenshots

This folder contains screenshots taken during the deployment and testing of the Dockerized Web Application on Azure.

The screenshots show the actual environment used for the project, including the virtual machine, networking configuration, running Docker containers, and the final application working in the browser.

---

## Included Screenshots

- **VM Overview**  
  Azure Virtual Machine running with a public IP assigned.

- **Network Security Group (NSG) Rules**  
  Inbound rules configured to allow access on ports 22, 80, 8080, and 8081.

- **Docker Containers Running**  
  Output of `docker ps` showing multiple containers running on the same VM.

- **Docker Image Build**  
  Terminal output confirming the successful build of the custom Docker image.

- **Application in Browser**  
  Browser view of the Dockerized web application accessed through the VM’s public IP.

---

These screenshots are included to show the setup, deployment process, and successful operation of the application in a real Azure environment.

